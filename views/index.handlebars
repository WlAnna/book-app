{{>header}}

<h2 class="page-header">Recently Added</h2>
<div class="book-grid">
    {{#each books}}
    <img class="book-cover" src="data:image/png;base64,{{this}}" alt="DB Image"  />
    {{/each}}
</div>


{{>footer}}